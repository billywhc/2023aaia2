#include<stdio.h>
#include<string.h>
int main(){
  char a[100],b[100];scanf("%s %s",&a,&b);
  if(strlen(a)<strlen(b))printf("-1");
  if(strlen(a)>strlen(b))printf("1");
  if(strlen(a)==strlen(b))printf("%d",strcmp(a,b));

  return 0;
}
